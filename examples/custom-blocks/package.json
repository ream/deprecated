{
  "devDependencies": {
    "github-markdown-css": "^2.4.1",
    "post-loader": "^1.1.0"
  },
  "dependencies": {
    "ream": "*"
  },
  "scripts": {
    "dev": "ream dev",
    "start": "npm run build && ream start",
    "build": "ream build",
    "generate": "ream generate",
    "deploy": "npm run generate && surge -p dist -d ream-custom-block.surge.sh"
  }
}
