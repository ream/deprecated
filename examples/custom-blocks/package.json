{
  "devDependencies": {
    "github-markdown-css": "^2.4.1",
    "post-loader": "^1.1.0"
  },
  "dependencies": {
    "unvue": "*"
  },
  "scripts": {
    "dev": "unvue dev",
    "start": "npm run build && unvue start",
    "build": "unvue build",
    "generate": "unvue generate",
    "deploy": "npm run generate && surge -p dist -d unvue-custom-block.surge.sh"
  }
}
